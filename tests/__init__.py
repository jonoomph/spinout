"""Test suite package for Spinout.

Subpackages expose logical groupings so individual suites can be run with
``pytest tests/controller``, ``pytest tests/planner`` or ``pytest tests/physics``
while ``pytest tests`` still runs everything.
"""
